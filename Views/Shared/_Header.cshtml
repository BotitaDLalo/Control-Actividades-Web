@using System.Web
<nav class="nav-bar">
    @*<nav class="navbar navbar-expand-sm navbar-light bg-white border-bottom">*@
    @*<style>
                /* Header icons: place in the top-right corner of the header (relative to full-width nav-bar) */
                .nav-bar {
                    position: relative;

        <div class="container-fluid d-flex align-items-center">
            @*<div class="me-auto">
                    <span id="page-title-header" class="fw-bold text-dark">@((ViewBag.PageHeader ?? "Header"))</span>
                </div>*@

    <div class="header-icons d-flex align-items-center">
        <div class="btn-modo-color">

        </div>
        <!--Botón modo oscuro-->
        <button id="cambiarModo" class="btn-modo-color btn btn-link p-2" type="button" title="ModoColor" aria-label="Cambiar tema">
            <p>
                <img class="modo-color-icono" src="~/Content/Iconos/icono-modo-claro.svg">
            </p>
        </button>

        <!-- Notifications button -->
        <button id="notificaciones-icono" class="btn btn-link p-2" type="button" title="Notificaciones">
            <span id="notificaciones-indicador" class="notiIndicador"></span>
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-bell-fill" viewBox="0 0 16 16">
                <path d="M8 16a2 2 0 0 0 2-2H6a2 2 0 0 0 2 2m.995-14.901a1 1 0 1 0-1.99 0A5 5 0 0 0 3 6c0 1.098-.5 6-2 7h14c-1.5-1-2-5.902-2-7 0-2.42-1.72-4.44-4.005-4.901" />
            </svg>
        </button>

        <!-- Panel de notificaciones (oculto inicialmente) -->
        <div id="notificaciones-panel" class="notificaciones-container" aria-hidden="true">
            <div class="p-2">
                <strong style="display:none;">Notificaciones</strong>
            </div>
            <div class="px-2">
                <small class="text-muted" style="display:none;">No hay notificaciones recientes.</small>
                <div class="lista-notificaciones"></div>
            </div>
        </div>

        <!-- Dropdown de opciones de usuario (usa Bootstrap dropdown) -->
        <div class="nav-item dropdown">
            <button class="btn btn-link p-2 dropdown-toggle" style="width:80px;" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false" type="button" title="Usuario">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-person-circle" viewBox="0 0 16 16">
                    <path d="M11 6a3 3 0 1 1-6 0 3 3 0 0 1 6 0" />
                    <path fill-rule="evenodd" d="M0 8a8 8 0 1 1 16 0A8 8 0 0 1 0 8m8-7a7 7 0 0 0-5.468 11.37C3.242 11.226 4.805 10 8 10s4.757 1.225 5.468 2.37A7 7 0 0 0 8 1" />
                </svg>
            </button>

            <div class="nav-sesion dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown" style="min-width:220px;">
                @*<div class="user-info px-3 py-2 d-flex align-items-center">*@
                <div class="elementos-perfil">
                    <img class="iconos1-nav me-2" src="@Url.Content("~/Content/Iconos/PERFIL-26.svg")" alt="Usuario" />
                    <div>
                        <div class="dropdown-item mb-0 nav-sesion-titulo">@User.Identity.Name</div>
                        <div class="nav-sesion-nombre">@(User.Identity.IsAuthenticated ? (User.Identity.Name ?? "") : "Invitado")</div>
                    </div>
                </div>
                <div class="dropdown-divider"></div>

                @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "px-3 py-2" }))
                {
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-outline-danger w-100 btn-cerrar-sesion" style="color: white; background-color: red">
                        Cerrar sesión
                    </button>
                }
            </div>
        </div>
    </div>

</nav>
