<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <!-- Use Inter font site-wide -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <style>
        html, body { font-family: 'Inter', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif; }
    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3 layout">
            <div class="container-fluid">

                <!-- Logo con enlace -->
                <a class="navbar-brand" href="@Url.Action("Index", "Alumno")">
                    <img src="@Url.Content("~/Content/Iconos/LOGO HORIZONTAL-26.svg")" alt="ActivIA Logo" class="navbar-icon">
                </a>

                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item dropdown">
                            <a href="#" class="nav-link text-dark" id="user-icon" onclick="toggleMenu(event)">
                                <img class="iconos-nav dropdown-toggle" src="@Url.Content("~/Content/Iconos/PERFIL-26.svg")" alt="Icono de Usuario" />
                            </a>

                            <div class="dropdown-menu" id="user-menu">
                                <div class="user-info">
                                    <img class="iconos1-nav" src="@Url.Content("~/Content/Iconos/PERFIL-26.svg")" alt="Icono de Usuario" />
                                    <p class="dropdown-item">@User.Identity.Name</p>
                                </div>

                                <!-- Formulario de cierre de sesión -->
                                @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { @class = "dropdown-item" }))
                                {
                                    <button type="submit" class="btn btn-danger btn-sm w-100">Cerrar Sesión</button>
                                }
                            </div>
                        </li>
                    </ul>
                </div>

            </div>
        </nav>
    </header>

    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()

            <br />
            <br />

        </main>
    </div>


    <!-- Modal de privacidad -->
    <div id="privacy-modal" class="modal">
        <div class="modal-content">
            <!-- Botón para cerrar (X) -->
            <span class="close-modal">&times;</span>

            <!-- Título del modal -->
            <h2 class="modal-title">Aviso de Privacidad</h2>
            <hr />

            <!-- Contenido del aviso -->
            <p class="modal-text">
                En Aprende+ valoramos tu privacidad. Este aviso describe cómo recopilamos, usamos y protegemos la información personal que compartes con nosotros. Si tienes alguna duda, no dudes en contactarnos.
            </p>

            <!-- Botón de aceptar -->
            <button class="modal-button" id="modal-accept">Aceptar</button>
        </div>
    </div>


    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
    @RenderSection("styles", required: false)
    <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</body>
</html>