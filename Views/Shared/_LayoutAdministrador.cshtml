<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    @Styles.Render("~/Content/css")
    @Styles.Render("~/Content/Variables")
    @Styles.Render("~/Content/Moleculas")
    @Styles.Render("~/Content/Componentes")
    @Styles.Render("~/Content/Dashboard/css")
    @Styles.Render("~/Content/Administrador/css")

    @RenderSection("styles", required: false)

    @Scripts.Render("~/bundles/modernizr")

    @*<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">*@

    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href="@Url.Content("~/Content/Iconos/logo2.svg")" type="image/x-icon" />
    <link href="~/Content/Componentes/carga.css" rel="stylesheet" />
    <link href="~/Content/Administrador/administrador.css" rel="stylesheet" />


    <!-- Use Inter font site-wide -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        html, body {
            font-family: 'Inter', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background: var(--bg-principal);
        }
    </style>

</head>
<body>
    <div class="layout">
        @Html.Partial("~/Views/Shared/_Header.cshtml")
        @Html.Partial("~/Views/Shared/_Sidebar.cshtml")
        <div class="page">
            @RenderBody()
        </div>
    </div>

    <script type="module" src="~/Scripts/Firebase/firebase.js"></script>

    <!--LAYOUT ADMIN-->
    <!--CARGA-->
    <div id="loader" class="carga">
        <div class="spinner"></div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/bootstrap")

    <!--
        <div class="container">
            <main role="main" class="pb-3">
                @*RenderBody()*@

                <br />
                <br />

            </main>
        </div>
        -->
    <!--
         <!-- Modal de privacidad
         <div id="privacy-modal" class="modal">
             <div class="modal-content">
                 <!-- Botón para cerrar (X)
                 <span class="close-modal">&times;</span>

                 <!-- Título del modal
                 <h2 class="modal-title">Aviso de Privacidad</h2>
                 <hr />

                 <!-- Contenido del aviso
                 <p class="modal-text">
                     En Aprende+ valoramos tu privacidad. Este aviso describe cómo recopilamos, usamos y protegemos la información personal que compartes con nosotros. Si tienes alguna duda, no dudes en contactarnos.
                 </p>

                 <!-- Botón de aceptar
                 <button class="modal-button" id="modal-accept">Aceptar</button>
             </div>
         </div>
    -->
    <script src="~/Scripts/Componentes/PantallaCarga.js"></script>
    <script src="~/Scripts/jquery.signalR-2.4.3.min.js"></script>
    <script src="~/signalr/hubs"></script>

    <script type="module">
        import { registrarTokenFcm } from '/Scripts/Firebase/firebase.js';
        document.addEventListener("DOMContentLoaded", function () {
            setTimeout(() => {
                registrarTokenFcm();
            }, 500);
        });
    </script>

    @RenderSection("scripts", required: false)
</body>
</html>
