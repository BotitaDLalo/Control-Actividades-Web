@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutDocente.cshtml";

    int docenteId = ViewBag.DocenteId;
}
<div id="docente-datos" data-docenteid="@docenteId"></div>

@* Mensaje de bienvenida *@       
<div class="misCursos-welcome">
    @if (User != null && User.Identity.IsAuthenticated)
    {
        <h3>Bienvenido, @User.Identity.Name</h3>
    }
    else
    {
        <h3>Bienvenido</h3>
    }
</div>

<div class="misCursos-header">
    <h2 class="misCursos-titulo">
        Mis cursos
        <img src="@Url.Content("~/Content/Iconos/FILTRO-26.svg")"
             alt="ActivIA Logo"
             class="misCursos-icono">
    </h2>

    <div class="misCursos-dropdown">
        <button class="misCursos-btn-crear" id="misCursos-dropdownBtn">CREAR</button>
        <div class="misCursos-dropdown-menu" id="misCursos-dropdownMenu">
            <button class="misCursos-dropdown-item" data-bs-toggle="modal" data-bs-target="#materiasModal">Gestionar Materias</button>
            <a class="misCursos-dropdown-item btn btn-link" href="@Url.Action("Grupos", "Docente")">Gestionar Grupos</a>
        </div>
    </div>
</div>

<br />
<br />

<div class="sin-actividades text-center mt-4">
    <h4 class="text-muted">Sin actividades pendientes</h4>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/docente")
    @Scripts.Render("~/bundles/docentegrupos")
}

@section Styles{
    @Styles.Render("~/Content/Docente/css")
}