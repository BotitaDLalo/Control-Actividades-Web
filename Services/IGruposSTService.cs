namespace ControlActividades.Services 
{ 
// Minimal placeholder interface to satisfy project references at root. 
public interface IGruposSTService 
{ 
// Intentionally empty; implementations live in Services.Grupos namespace. 
} 
} 
